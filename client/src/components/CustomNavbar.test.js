import { render, screen } from '@testing-library/react';
import CustomNavbar from './CustomNavbar';



test('renders login button when not logged int', () => {
    render(<CustomNavbar />);
    lo
});
  